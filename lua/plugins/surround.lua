-- ~/.config/nvim/lua/plugins/surround.lua
return {
	"kylechui/nvim-surround",
	event = "VeryLazy", -- Loads in the background after the UI is drawn
	opts = {
		-- The defaults are industry standard, no extra config needed!
	},
}
